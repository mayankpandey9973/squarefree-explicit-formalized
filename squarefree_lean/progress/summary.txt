SQUAREFREE FORMALIZATION — PROGRESS SUMMARY (2026-06-18)
=========================================================

BUILD: GREEN, full project (`lake build`, 8548 jobs). Tracked stubs: 0 — COMPLETE.
  theorem_10_1 axioms = [propext, Classical.choice, Quot.sound] — AXIOM-CLEAN, no sorryAx.

PERF (2026-06-18): full clean project build 48m17s → 22m29s (~53% faster), still axiom-clean.
  ~95 modules sped up 2–11× by nlinarith→`nlinarith only`/`linarith only` (the linear/simple-goal-in-
  huge-rpow-context anti-pattern); ~60 maxHeartbeats bumps removed/lowered (incl. 25.6M, 10M, 6.4M).
  WARNINGS 406 → 0 (all cleared: deprecated APIs, unused vars _-prefixed, no-op/dead tactics, <;>→;).
  Use LEAN_NUM_THREADS=10 (no -j flag); never `lake clean`. Parallelize chain passes via `lake env lean`.
  See CLAUDE.md §1a. Residual cost floor = simp/typeclass/bignum + the linarith certificate oracle (intrinsic).

HYPOTHESIS STRIP (2026-06-19): ~147 vestigial unused hypotheses removed, statement-level, with all
  caller updates threaded tree-wide (cascade to fixpoint). Still GREEN (full build 22m21s, 8548 jobs,
  0 warnings) + axiom-clean [propext, Classical.choice, Quot.sound]. Breakdown:
    - Stage 1 (private decls): ~67 binders (commit 02b67a0).
    - Stage 2 (public lemmas, theorem/lemma only — def API-symmetry params left intact): ~80 binders.
      Candidate signal = `_`-prefixed binders in public sigs (the warning sweep already flagged them);
      37 owner files / 7 file-disjoint waves, parallel agents, per-file `lake env lean` verify.
  Method: delete the `_`-binder, drop the matching positional arg at every caller, `_`-prefix orphaned
  PUBLIC caller binders / strip orphaned PRIVATE ones (cascade up). REVERT when the "unused" binder is
  silently consumed by a tactic context-search (`positivity`/`linarith`/`field_simp` side-goal) — 3 such
  skips in Stage 2 (welim hB0, expanding_and_variation _hK, inD_distInt_Shat_neg _hb). Faithfulness
  firewall: never strip from `prop_*`/`lemma_<digits>`/`theorem_*` or frontier/structure interfaces.

  ✅ §7 Φ″ ENDGAME CLOSED (2026-06-17, commit 589b8df): the last sorry
     (sec7_zero_few_critical Φ″ branch) filled by two new lemmas in Sec7ZeroScale:
       - sec7_zero_errScale_m3_le_Cbase  (Cbase-form m=3 error budget, 6e-13 ≤ 1e-12)
       - sec7_zero_deriv2_few  (weight-r⁴ Rolle; principal collapses to the C-monomial
         via sec7_principal_weighted_collapse2, coeff −1105/64; two-piece error
         dominated ≈101×: 1.70e-6 vs 1.729e-4 · R·Cbase ⇒ ≤1 zero ≤ KZero).
     Whole tree: zero sorry/admit/axiom. The formalization of theorem_10_1 is DONE.

  ✅ sec7_phase_build CLOSED (2026-06-16): = sec7_phase_construct (hG10x-free) + hnear reorder.
     hG10x (G·U^10 ≤ x, regime-FALSE on-strip) DELETED tree-wide; sec7_relErrF re-pinned to the
     power-saving X^{-19/100}. Cascade: Sec7ZeroHyp field hsub2F→hGΩ5F (discharged by new
     BoxSum.sec7_zero_hGΩ5F: G·Ω⁵·X^{-19/100} ≤ 1/cSub from band Ω≤U + admissibility g+5u≤1/100
     + X^{18/100}≥(2^24)^18≥10^55); hUbig (10^33≤U, needed for the 10^143 relErrF discharge)
     threaded merger→dblock_on_strip→prop_7_3→{prop_7_1→triple_split→nonzero_wide_count→
     {nonzero_pieces,nonzero_count_78,nonzero_relErrF_small}, ra_data_pack→phase_build→phase_construct}.
     BoxSum now imports Sec7PhaseConstruct.

§7 ENDGAME PROGRESS (2026-06-16) — 4 original sorries → 2 (and one of those is half-closed):
  ✅ phiContDiff PROVEN. Major architectural fix: the §7 zero-count engines required GLOBAL
     `ContDiff ℝ 2 Φ`, but Φ is `Function.invFun`-built ⟹ only `ContDiffOn`. Reworked to mirror
     §5's `step2_subset_count`: field/wrapper/Sec7ZeroHyp.hcd → `ContDiffOn … (Ioo(R/144-1)(40R+1))`;
     two new adapters bump-extend internally — `sec7_prop43_local_contDiffOn` (N19/prop43, Sec7PhiSmooth)
     and `sec7_bands_count_slack_split_contDiffOn` (N13/bands, Sec7ZeroScale). The field ALSO needed two
     dropped hyps (it was unprovable as a bare sorry): `sec7_jBand P S j` + `sec7_phiBound` (box+|ξ|≤hSum,
     new Prop in Sec7Defs). phiContDiff proven pointwise (f2D=ftil smooth ∀r>0; f1D/f3D need r∈rWin).
  ✅ sec7_phase_round_inverse_margin PROVEN. Also under-hypothesized: needed near-integrality
     `distInt(Ffun a d) ≤ 2H/d²` (Prop 3.2, threaded into sec7_phase_construct's output conjunct) AND
     `hpert : 2H/D² ≤ F/1000` (discharged in-construct via sec7_ra_HA2_large + hAD). Ffun-IVT/MVT,
     constant 147456 ≤ cdMar=10⁷.
  ◐ phiFewCritical — RELOCATED + HALF-CLOSED. Was false/under-hyp'd as a Sec7Phase field (needs
     jBand+phiBound+hρ₀:ρ₀=0+carry/fiber/strip facts, and the strip facts aren't Sec7Phase fields).
     RESTRUCTURED: removed from Sec7Phase AND Sec7ZeroHyp; now a derived theorem
     `sec7_zero_few_critical (ME)(Hyp)` in Sec7ZeroScale, filled into the N13 count. Construct is now
     sorry-FREE. ✅ generic Rolle `sec7_zeros_subsingleton_of_deriv_ne` + ✅ Φ′ branch `sec7_zero_deriv_few`
     (weight r³ collapses ρ₀=0 principal to one (65/16)C·R·y⁻⁹ᐟ⁴ monomial dominating the ≤2-order remainder,
     Rolle ⟹ ≤1). ⛔ Φ″ branch OPEN (1 sorry, ~1984): `(r⁴Φ″)′` needs ORDER-3 (`iteratedDeriv 3 Φ`), but
     `sec7_leib_deriv` (Sec7ErrAux:82, m<2) + `sec7_err_deriv_bound` (Sec7ErrBound:175, m≤2) cap the chain.
     Inputs exist (ra_e*D_deriv m<5); FIX = extend leib_deriv→m<3 + err_deriv_bound→m≤3 (cross-file:
     Sec7ErrAux/ErrBound + the PhiJet/A/Q chains), then mirror Φ′ with weight r⁴.
  ⛔ sec7_phase_build — N24 assembly: construct the Sec7Phase + thread hnear (inDa_distInt_Ffun) into
     sec7_phase_construct's now-hnear-carrying margin conjunct. Independent of the Φ″ blocker.

DONE earlier: §1, §4, §5 (prop_5_1 axiom-clean C=10⁴¹⁵), §6 (prop_6_1), §8/§9 spine.
